---
build:
    local: true
    commands:
    - gcc main.c -o main.exe -g
    - mars a data/fibonacci.asm dump .text Binary data/text.bin
    - mars a data/fibonacci.asm dump .data Binary data/data.bin
    - ./main.exe data/text.bin data/data.bin
